from setuptools import setup, find_packages

setup(
    name='NewYear',
    version='0.0.1',
    description='',
    packages=find_packages(),
    install_requires=[
        'ascii-magic',
        'colorama',
        'tqdm',
    ],
)
